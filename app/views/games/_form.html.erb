<div id="board">
  <%= form_tag game_path(@game), method: :patch do %>
    <table>
      <% @game.state.chars.each_with_index do |symbol, index| %>
        <% if index % 3 == 0 %>
          <tr>
        <% end %>
        <td>
          <%= radio_button_tag :position, index, @game.state[index] == '-' %>
        </td>
        <% if (index + 1) % 3 == 0 %>
          </tr>
        <% end %>
      <% end %>
    </table>

    <p>Escolha 'X' ou 'O' e clique em uma célula vazia para fazer seu movimento.</p>

    <label>
      <%= radio_button_tag :symbol, 'X' %>
      X
    </label>
    <label>
      <%= radio_button_tag :symbol, 'O' %>
      O
    </label>

    <%= submit_tag 'Fazer Movimento' %>
  <% end %>
</div>
